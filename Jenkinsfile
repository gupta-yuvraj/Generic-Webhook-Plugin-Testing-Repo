pipeline {
    agent any
    stages {
        stage('Generic Webhook Trigger Plugin Test') {
            steps {
                echo "Generic Webhook Trigger Plugin Test"
            }
        }
    }
}
